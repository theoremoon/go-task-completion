#compdef task

_task() {
    local tasklist=$(task --list-all)
    if [[ "$?" -eq 0 ]]; then
        local tasks=$(echo "$tasklist" | grep '^\*' | awk '{print $2}' | sed 's/.$//')
        describe task tasks
    fi
}
